# Legacy documentation (pre-Snap web app)

The Markdown guides in this folder (upload modes, folder zip, architecture, etc.) were written for the **previous full Next.js dApp** with in-browser wallet, swap UI, and uploads.

**The current product** is the **MetaMask Snap** in `snap/` and the small install page in `src/app/`. Behavior and UI differ; use these files only for historical or technical context (Swarm, Bee, stamps concepts are still valid).

The up-to-date entry point is the [root README](../README.md).
